{% extends 'base.html.twig' %}



{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {% if img!=null %}
    <img src="/images/news/{{img}}" width="200"/>
    {% endif %}
    {{ form_end(form) }}
{% endblock %}